name: Deploy to GitHub Pages

on:
  push:
    branches: [main] # Запускати при пуші в гілку main
  pull_request:
    branches: [main]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v3

      - name: Install Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "18" # Використовуємо 18 версію Node

      - name: Install dependencies 📦
        run: npm install

      - name: Build 🏗️
        run: npm run build # Запускаємо команду 'vite build'

      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          branch: gh-pages # Гілка, куди будуть завантажуватись файли
          folder: dist # Папка, яку Vite збирає (де лежить готовий сайт)
